#include "doubleLinkList.h"
#include <stdio.h>
#define CHEDCKOUT(ptr)          \
        do                      \
        {                       \
            if(ptr == NULL)     \
            {                   \
                exit(0);        \
            }                   \
        } while (0);            
        
int doubleLinkListInit(DoubleLinkList *pList)
{
    
    return 0;
}